# date-share

데이트 코스 에이전트(텔레그램)용 카카오톡 공유 정적 페이지.

텔방 메시지의 "📤 카톡으로 공유" 버튼이 이 페이지를 연다.
URL 해시(`#<base64url>`)로 전달된 코스 마크다운의 `[명칭](url)` 링크를
생(生) URL로 변환한 뒤 `navigator.share()`로 카카오톡에 공유한다.

코스 내용은 URL 해시로만 전달되며 서버/저장소에 남지 않는다.
